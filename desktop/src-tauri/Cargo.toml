[package]
name = "vitr"
version = "0.1.0"
description = "vitr standalone UI"
authors = ["Blood"]
edition = "2021"

[lib]
name = "vitr_lib"
crate-type = ["cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
tauri = { version = "2.8.5" }
